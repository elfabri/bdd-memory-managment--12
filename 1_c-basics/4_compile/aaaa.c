/*
 * Run the code. Notice that the "starting..." message never prints.
 * Fix the bug by removing the nonexistent function call.
 */

#include <stdio.h>

int main() {
    printf("starting sneklang tool\n");
    printf("finished sneklang tool\n");
    return 0;
}
