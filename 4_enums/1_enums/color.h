/* The Sneklang graphics library needs to represent colors.
 * Create a Color enum (and the color_t typedef) with RED,
 * GREEN, and BLUE values, in that order.
 */

typedef enum Color {
    RED,
    GREEN,
    BLUE,
} color_t;
